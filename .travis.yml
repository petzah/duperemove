sudo: required
language: generic

services:
  - docker

script:
  - wget -O- https://raw.githubusercontent.com/petzah/travis.debian.net/multiarch/script.sh | sh -

branches:
  except:
    - /^debian\/\d/
